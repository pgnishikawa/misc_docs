# `claude_code_global_rag_proposal.md` レビュー

## 総評
設計思想（PDF全量投入 → 構造化Markdownの局所検索へ移行）自体は正しい方向で、トークン消費削減には確実に効果があります。ただし、以下の点は「動くには動くが、想定通りのトークン削減効果や運用性が出ない」リスクがあるため、実装前に見直すことを推奨します。

---

## 1. これは厳密には「RAG」ではない

タイトルに RAG（Retrieval-Augmented Generation）とありますが、実体は「ディレクトリ構造 + CLAUDE.mdの行動指示 + grep」による**キーワード検索**であり、埋め込みベクトルによる意味検索ではありません。

- **問題点**: `grep` は完全一致・正規表現ベースです。“USART” で書かれたMarkdownに対して "UART" や "シリアル通信" のような表記ゆれ・言い換えで検索すると、AIが正しいファイルに辿り着けない可能性があります。
- **改善案**: 各Markdownファイルの先頭にfrontmatterでエイリアス/キーワードを持たせる。
  ```yaml
  ---
  keywords: [USART, UART, シリアル通信, 非同期通信]
  ---
  ```
  これにより `grep -i` で表記ゆれをある程度吸収できます。真の意味検索が必要になった場合のみ、埋め込みベクトルDB（MCP経由）の導入を検討すればよく、初期構成では過剰投資です。

---

## 2. CLAUDE.mdの自動読み込みとトークン消費の関係を誤解している可能性

Claude Codeは**プロジェクトルート（および親ディレクトリ）のCLAUDE.mdをセッション開始時に自動読み込み**します。加えて、**サブディレクトリ配下のCLAUDE.mdは、Claudeがそのディレクトリ内のファイルを実際に読みに行った際に追加読み込みされる**仕組みがあります。

- **問題点**: 提案書 5章では、巨大な参照インデックスを「各プロジェクトルートのCLAUDE.md」に丸ごと書く想定になっています。これだと毎セッション、無関係なMCUファミリの情報まで含めて自動的にコンテキストへ乗ってしまい、狙いである「トークン消費の極小化」と矛盾します。
- **改善案**:
  - ルートCLAUDE.mdは**数行の要約**（グローバルVaultの場所とルールのみ）に留める。
  - MCUファミリごとの詳細インデックスは `00_Vault/01_MCUs/STM32F4/CLAUDE.md` のように**サブディレクトリ側に配置**し、Claudeがそのディレクトリを触ったときだけ読み込まれるようにする。
  - さらに、Claude CodeのCLAUDE.mdは `@path/to/file` 記法でファイルをインポートできるため、各プロジェクトのCLAUDE.mdに毎回同じ参照ブロックをコピペする代わりに `@~/global_context/00_Vault/CLAUDE.md` のように1行でインポートし、実体は一箇所で管理するほうが二重管理を避けられます（提案書6章の「二重管理の手間がない」という主張は、実はこの参照ブロック自体が複数プロジェクトにコピペされる設計になっており矛盾しています）。

---

## 3. 権限プロンプトの摩擦（運用上見落とされがち）

Claude Codeはプロジェクト外の絶対パス（`~/global_context/...`）へのアクセス自体は可能ですが、プロジェクトディレクトリ外へのRead/Grepは**セッションごと・パスごとに許可プロンプトが出る**ことがあります。

- **問題点**: 「必要な時だけ局所検索」という設計は、逆に言うと**頻繁に`~/global_context/`配下へアクセスする**ということです。許可設定なしだと、検索のたびに確認ダイアログが出てワークフローが止まり、結果的に「面倒だから全部読ませよう」という後退を招きかねません。
- **改善案**: `~/.claude/settings.json`（グローバル設定）に、`~/global_context/**` に対するRead/Grep許可をあらかじめ登録しておく。これにより毎回の確認なしで軽量検索が機能します。

---

## 4. PDF→Markdown変換フローが手作業前提でボトルネックになる

4章の手順は「PDFからテキストをコピーしてWeb版Claudeに貼り付ける」という完全手動プロセスです。1,000ページ超のデータシート全体をこの方法で構造化するのは非現実的な工数になります。

- **改善案**: Claude Code自身の`Read`ツールは**PDFファイルを直接読み込み可能**（ページ範囲指定つき、1回最大20ページ程度）です。Web版へのコピペを介さず、Claude Code上でスクリプト的にページ範囲をループしながら「このページ範囲をMarkdown化して`00_Vault/`に保存」という指示を繰り返す方が、変換ルールの一貫性も保て、工数も削減できます。章ごとに並列でサブエージェントに投げる運用も検討可能です。

---

## 5. Markdown化後のデータ鮮度・正誤検証の仕組みがない

一度Markdown化すると、それが「正」として扱われ続けます。Errata更新やシリコンリビジョン変更があった場合、元PDFとの乖離に気づく仕組みが提案書にありません。

- **改善案**: 各Markdownファイルに変換元のPDFページ番号・版数・変換日を記載しておく（例: `> Source: RM0090 Rev19, p.450, converted 2026-08-23`）。これにより、AIが古い情報を参照した際にユーザーが検証しやすくなります。

---

## まとめ

| 項目 | 評価 |
|---|---|
| 設計思想（局所検索への移行） | 妥当。トークン削減効果は期待できる |
| 「RAG」という呼称 | 実体はgrepベースの検索であり、意味検索ではない点に注意 |
| CLAUDE.md運用 | サブディレクトリ配置と`@`インポートを使わないと、狙いと逆にトークンを消費する |
| 権限周り | 事前のpermissions設定がないと運用がストレスになる |
| PDF変換フロー | 手動コピペは非現実的。Claude CodeのPDF直接読み込み機能で自動化すべき |
| 鮮度管理 | ソース参照（版数・ページ）を残す仕組みが必要 |

総合すると、**「動くには動くが、CLAUDE.mdの配置方法と権限設定を見直さないと、期待するほどトークンは節約できない」**というのが結論です。特に2章（CLAUDE.mdの自動読み込み範囲）は設計の根幹に関わるため優先的に見直すことを推奨します。
